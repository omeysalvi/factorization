Add the possible optimization of factor not being greater than sqrt
I think the code can run on multiple cores at the same time to be parallelized

Assumptions made:
only two factors - both are prime
both factors have approximately the same number of digits which is approximately half of number you are factoring

further reading:
rectangular hyperbola of type xy = c
fermat's approximation

Alogrithm prompt:
In the attached research paper 2022_JAMCS_94759, 4.1 theoretical essences => proposition 3, I believe we can use the property that beta (divisor ratio) is greater than that particular value to write a algorithm that narrows down the range to search for prime divisors. Since we know that if the divisor ratio is greater than the second value that means there is no natural number odd pair of prime divisors in that range as the paper calls it. I want to impose a value on the divisor ratio and then remove ranges over which it is greater than the second value. Starting from the middle of the equation xy = N where N is the subprime, I want to do a binary search in both directions upto a particular range. So root N, root N is the center of the rectangular hyperbola defined by xy=N. It will have a 45 degree angle if the hyperbola is mapped in polar coordinates. So, find the upper and lower point for say 5 degrees. Then starting from the center go both up and down to set values of alpha and k. If the condition is not met, stop meaning there is no odd pair in that range or our assumed starting value of the divisor ratio is wrong. If it is met though, further split the range in half and repeat over both smaller arcs. repeat till the range is less than a particular value that I'll set called ""calculable range". Once the value is in a calculable range, run a sieve to remove even and prime multiples upto a certain value. Then with the remaining numbers, try to find the factor for the original N.
